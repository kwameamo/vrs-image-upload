import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "VRS Image Upload",
  description: "The Vehicle Registration System (VRS) Image Upload platform is a secure, efficient web application designed specifically for DVLA stations across Ghana. This user-friendly system allows authorized staff to capture, store, and organize vehicle chassis documentation through a streamlined digital workflow.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
